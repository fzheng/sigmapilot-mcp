# SigmaPilot MCP Server - Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# Auth0 Configuration (Required for production)
# =============================================================================

# Your Auth0 domain (e.g., your-tenant.auth0.com)
AUTH0_DOMAIN=your-tenant.auth0.com

# API Identifier from Auth0 (the "audience" value)
# This should match the API you create in Auth0 Dashboard
AUTH0_AUDIENCE=https://sigmapilot-mcp.example.com

# Public URL where your MCP server is accessible
# For Railway: https://your-app-name.up.railway.app/mcp
# For local development: http://localhost:8000/mcp
RESOURCE_SERVER_URL=http://localhost:8000/mcp

# =============================================================================
# Server Configuration
# =============================================================================

# Server port (Railway sets this automatically)
PORT=8000

# Server host (use 0.0.0.0 for production to accept external connections)
HOST=0.0.0.0

# =============================================================================
# Optional: Debug Mode
# =============================================================================

# Set to any value to enable debug logging
# DEBUG_MCP=1
